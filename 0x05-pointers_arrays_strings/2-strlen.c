#include "main.h"

/**
 * _strlen - returns the length of a string
 *
 * @s: pointer to 1st value / input
 */

int _strlen(char *s)
{
	int count;

	for (count = 0; *s != '\0'; ++count)
    
	return count;
}
